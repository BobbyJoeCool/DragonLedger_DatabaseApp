# Azure SQL connection string (Prisma format)
DATABASE_URL="sqlserver://dragonledger-sql.database.windows.net:1433;database=dragonledger-db;user=z002p25;password=AlphaBetaGamma85;encrypt=true;trustServerCertificate=false;connectionTimeout=30"

# Single shared app password for write access
APP_PASSWORD="NukeUm85"

# Client origin for CORS (local dev: http://localhost:5173)
CLIENT_ORIGIN="http://localhost:5173"

# Express server port
PORT=3000
